# Note: The main playbook defines these handlers inline. This file is kept for reference.
- name: restart gpsd
  ansible.builtin.systemd:
    name: gpsd
    state: restarted

- name: restart chrony
  ansible.builtin.systemd:
    name: chrony
    state: restarted
